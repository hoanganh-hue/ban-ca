{
  "name": "@underwater-game/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and constants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}